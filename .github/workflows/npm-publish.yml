name: NPM PUBLISH
on:
  push:
    branches: [main]
jobs:
  build:
    name: "npm publish library"
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v2.3.2

      - name: � Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: 🔨 Build Project
        run: bun install --frozen-lockfile && bun run build

      - name: Setup npm authentication
        run: echo "//registry.npmjs.org/:_authToken=${{ secrets.NODE_AUTH_TOKEN }}" > dist/.npmrc

      - name: Publish npm package
        run: cd dist && bun publish --access public --ignore-scripts
